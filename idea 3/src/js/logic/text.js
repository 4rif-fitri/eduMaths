export function updateText(text){
	let dialog = document.querySelector(".dialog p")

	if (typeof (text,Object))
 		dialog.textContent = text.text
	
	else	
		elsedialog.textContent = text

	return ""
}